docker_networks:
  cd:
    subnet: 172.18.1.0/24
  ci:
    subnet: 172.18.2.0/24

docker_images:
  # This duplicate allows to ensure uniqueness
  - foo
  - foo

  - bar
  - quux

docker_containers:
  web_service:
    foo:
      host: foo.domain.tld
      app_port: 10000

  non_web_service:
    bar:
      network: ci
